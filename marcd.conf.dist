[mysql]
; Database credentials
hostname = localhost
username = marc
password = konko
database = marc

[security]
; Drop privileges to user/group
;user = marc
;group = marc

[general]
; Enable built-in relay.
;relay = true

; IP to listen on.
;listen = 0.0.0.0

; Directory where persistent data is stored.
;datadir = /var/lib/marcd
